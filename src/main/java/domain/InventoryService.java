package domain;

import org.springframework.stereotype.Service;

@Service
public class InventoryService {

    // Método de exemplo para verificar o inventário
    public boolean checkInventory() {
        // Lógica para verificar o inventário
        // Aqui, estamos simulando um inventário suficiente para o exemplo
        return true;
    }
}
